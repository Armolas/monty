#include "monty.h"

/**
 * queue - shifts mode to queue
 * @stack: top of stack
 * @line_number: line number
 */
void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	myglob.mode = 1;
}
