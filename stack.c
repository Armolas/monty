#include "monty.h"

/**
 * stack - shifts mode to stack
 * @stack: top of stack
 * @line_number: line number
 */
void stack(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	myglob.mode = 0;
}
